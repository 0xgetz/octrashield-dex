# Private key for transaction signing (base64 encoded)
# Generate your private key and encode it to base64, then replace the value below.
# WARNING: Never share your actual private key or commit it to version control.
PRIVATE_KEY_B64=your_base64_encoded_private_key_here
